/*
==================================================
  SLAYER TERMINAL - PAPER (pages/paper/Paper.tsx)

  The door onto the paper desk — its own tab under
  the Weigher (Noah, 2026-09-19). Everything the
  desk does lives in PaperDesk.
==================================================
*/

import PaperDesk from './PaperDesk';

const Paper = () => <PaperDesk />;

export default Paper;
